First Steps:
1. Extract all this stuff to a folder somewhere.
2. Install python 2.7 and python 3.whatever
3. pip install lmfit, probably by 'py -2 -m pip install lmfit'.
4. pip install pmw, same way


Using git:
make sure git bash is in the actual repository (not the local folder)
